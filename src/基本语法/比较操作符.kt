package 基本语法

fun main(args: Array<String>) {
   /*
    a > b	a.compareTo(b) > 0
    a < b	a.compareTo(b) < 0
    a >= b	a.compareTo(b) >= 0
    a <= b	a.compareTo(b) <= 0
    所有的比较都转换为对 compareTo 的调用，这个函数需要返回 Int 值*/
}

